{
  "name": "git-partial-push",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --target bun --compile --outfile dist/git-partial-push ./bin/git-partial-push.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "cmd-ts": "^0.13.0"
  }
}
